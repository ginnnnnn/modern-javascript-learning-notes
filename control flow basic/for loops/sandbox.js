//for loops

for (let i = 0; i < 5; i++) {
    console.log('in loops', i)
}
// no semi colon ; for for loops

console.log('loops finished')// this will execute when the loops done

const members = ['luke', 'joe', 'mary', 'terry'];

for (let i = 0; i < members.length; i++) {
    console.log(members[i])
}