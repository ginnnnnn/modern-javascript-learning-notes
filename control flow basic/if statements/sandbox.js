//if statements or condition statements

const age = 25;

if (age < 30) {
    console.log(age);
}

const names = ['ken', 'ryo', 'saga', 'chun-li', 'blanka'];

if (names.length > 3) {
    console.log("that's a lot of ninjas");
}

const password = 'pass';

if (password.lengh >= 8) {
    console.log('that password is long enough!')
} else {
    console.log('that password is not long enough')
}